import * as React from "react";
import {Card, CardContent, CardHeader} from "@mui/material";

export default () => (
    <Card>
        <CardHeader title="Welcome to Administration"/>
        <CardContent>Namaskaram</CardContent>
    </Card>
);